syntax = "proto3";

package protocol;

message ProgramInput {
    uint32 player_id = 1;
    repeated uint32 register_cards_choices = 2;
}

message StartPositionInput {
    uint32 player_id = 1;
    uint32 start_position_id = 2;
}